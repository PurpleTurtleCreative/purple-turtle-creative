{
  "name": "purple-turtle-creative",
  "version": "1.0.0",
  "description": "Custom theme for Purple Turtle Creative WordPress website.",
  "author": "Michelle Blanchette",
  "devDependencies": {
    "node-sass": "^5.0.0",
    "terser-folder": "^2.0.0"
  },
  "scripts": {
    "watch": "node-sass assets/styles/sass/ -o assets/styles/ --source-map true --output-style compressed --indent-type tab --indent-width 1 -wr",
    "compile:css": "node-sass assets/styles/sass/ -o assets/styles/ --source-map true --output-style compressed",
    "compile:js": "terser-folder assets/scripts/js/ --each --output=assets/scripts/ --config-file=./terser.json"
  }
}
